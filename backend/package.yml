# Root package: 共有基盤
# 含むもの: ApplicationController（ベース）, ApplicationRecord, ApplicationJob, ApplicationMailer
# 依存: なし（最も基底）。認証は packs/identity の Authenticatable concern 経由で個別 include する
# See docs/adr/007-adopt-modular-monolith.md

enforce_dependencies: true

dependencies: []
