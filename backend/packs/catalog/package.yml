# Catalog pack: 商品カタログ
# 公開対象: Product, ProductImage, ProductsController
# 依存: identity（出品者ユーザー参照のため）, root（ApplicationRecord）
# See docs/adr/007-adopt-modular-monolith.md

enforce_dependencies: true

dependencies:
  - "."
  - "packs/identity"
